describe("Scheduler Tests", function() {
    it("Cria o Schedule com os intervalos", function() {

        var track = "09:00AM Writing Fast Tests Against Enterprise Rails 60min";

        var scheduler = {
            this.tracks = [track];
            genera
        };

        expect(true).toBe(true);
    });
});